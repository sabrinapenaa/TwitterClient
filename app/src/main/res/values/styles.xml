<resources>

    <style name="AppBaseTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
        <item name="colorPrimary"> #1DA1F2</item>
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>


    <style name="counterText" parent="TextAppearance.AppCompat.Small">
        <item name="android:textColor">@color/twitter_blue</item>
        <!--other parameters that you want to change -->
    </style>
    <style name="counterOverride" parent="TextAppearance.AppCompat.Small">
        <item name="android:textColor">@color/red</item>
        <!--other parameters that you want to change -->
    </style>
    <style name="Button.TwitterBlue" parent="ThemeOverlay.AppCompat">
        <item name="colorAccent">@color/twitter_blue</item>
    </style>
</resources>
